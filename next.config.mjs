import { initOpenNextCloudflareForDev } from "@opennextjs/cloudflare";

// Initialize OpenNext for development (enables Cloudflare bindings in dev mode)
initOpenNextCloudflareForDev();

const nextConfig = {
  /* config options here */
};

export default nextConfig;
